## Overview

This notebook demonstrates how to read and process metadata using Python.

### Execution
- Load a Dublin Core XML file
- Understand key metadata fields (e.g., title, creator, description, identifier)
- Parse XML metadata step by step using Python
- Convert metadata into a structured format (Pandas DataFrame)

### Key focus areas
- Structure of metadata in XML
- How machines interpret metadata
- The role of metadata in supporting **Findability** and **Reusability** (FAIR principles)
